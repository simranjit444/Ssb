
  # Create requested design

  This is a code bundle for Create requested design. The original project is available at https://www.figma.com/design/ytySmXfz7PW0cuB4pVb6xP/Create-requested-design.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  